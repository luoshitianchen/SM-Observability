# SM-Observability 安全基线（SECURITY BASELINE）

> 服务：sm-observability（可观测性服务）
> 版本：v1.0 ｜ 适用环境：dev / staging / prod
> 编制依据：等保 2.0 三级、ISO 27001:2022、企业内部安全规范

## 1. 安全基线概述

本文件定义 SM-Observability 在容器化部署（Kubernetes）与企业网络环境下必须满足的安全控制项。所有控制项分为"必须（Must）"与"建议（Should）"两级，必须项未通过不得上线生产。

基线覆盖：身份认证、网络隔离、数据加密、容器加固、密钥管理、审计日志、漏洞管理七大领域。

## 2. 等保 2.0 三级控制映射表

| 控制点 | 实现方式 | 证据 |
|---|---|---|
| 身份鉴别 | 内部服务间 mTLS + JWT 校验，管理员接入 SSO | 配置项 SM_INTERNAL_API_KEY、JWT 中间件日志 |
| 访问控制 | RBAC 角色矩阵 + NetworkPolicy 命名空间隔离 | NetworkPolicy YAML、RBAC 配置 |
| 安全审计 | 审计日志异步转发至 SM-Audit-Log-Center，保留 ≥180 天 | 审计中心查询记录、日志保留策略 |
| 入侵防范 | 容器只读根文件系统、能力全量 drop、非 root 运行 | Deployment securityContext |
| 恶意代码防范 | 镜像 Trivy 扫描 + SBOM 生成 + 漏洞阻断流水线 | CI 扫描报告、sbom.json |
| 数据完整性 | 国密 SM3 摘要校验关键数据传输 | SM3 签名验证日志 |
| 数据保密性 | 国密 SM4-CBC 加密敏感字段，密钥经 KMS 注入 | ExternalSecret 配置、SM4 加解密模块 |
| 个人信息保护 | 敏感字段脱敏输出，最小化采集 | 数据分类分级表（见第 4 节） |

## 3. ISO 27001 控制映射表

| 控制域 | 关键控制 | 实现方式 |
|---|---|---|
| A.5 组织安全 | A.5.34 信息在供应商服务中 | 供应商 SLA 合同、第三方安全评估 |
| A.8 人员安全 | A.8.2 保密协议 | 员工入职/调岗/离职保密协议签署 |
| A.8 实物安全 | A.8.15 访问场所 | 数据中心门禁、机房视频监控 |
| A.8 设备安全 | A.8.20 云服务安全 | 云厂商安全基线、CSPM 扫描 |
| A.8 设备安全 | A.8.23 信息删除/擦除 | 退役介质消磁/物理粉碎记录 |
| A.9 访问控制 | A.9.2 用户访问管理 | SSO + RBAC + 定期权限复核 |
| A.9 访问控制 | A.9.4 特殊权限 | DBA/管理员权限双人审批 |
| A.10 密码学 | A.10.1 密码控制 | 国密 SM2/SM3/SM4 全链路应用 |
| A.12 运营安全 | A.12.4 日志与监控 | 集中日志平台（Loki）、告警规则 |
| A.12 运营安全 | A.12.6 漏洞管理 | 月度漏洞扫描、补丁 SLA |
| A.13 通信安全 | A.13.1 网络控制 | NetworkPolicy、WAF、零信任网关 |
| A.16 事件管理 | A.16.1 信息安全事件 | 应急响应预案、事件复盘报告 |
| A.17 业务连续性 | A.17.1 信息系统业务连续性 | 多可用区部署、DR 演练 |
| A.18 合规 | A.18.1 法律法规符合性 | 等保测评、个人信息影响评估 |

## 4. 数据分类分级表

| 数据级别 | 示例字段 | 存储要求 | 传输要求 | 访问要求 |
|---|---|---|---|---|
| 公开（L1） | 服务元数据、API 文档 | 明文 | HTTP/HTTPS | 全员可读 |
| 内部（L2） | 业务统计、非敏感配置 | 明文落库 | HTTPS | 企业内网 + 认证 |
| 机密（L3） | 业务数据、用户行为 | SM4 加密落库 | mTLS | RBAC 授权 + 审计 |
| 绝密（L4） | 密钥、密码、令牌 | KMS/HSM 内存级保护 | 禁止出网 | 双人授权 + 实时审计 |

## 5. RBAC 权限矩阵

| 角色 | 资源 | 读 | 写 | 管理 |
|---|---|---|---|---|
| 匿名访客 | 公开 API | ✅ | ❌ | ❌ |
| 业务用户 | 业务数据 | ✅（自有） | ✅（自有） | ❌ |
| 业务管理员 | 业务数据 | ✅（全部） | ✅（全部） | ❌ |
| 平台运维 | 服务配置 | ✅ | ✅ | ❌ |
| 安全管理员 | 审计日志 | ✅（全部） | ❌ | ✅（审计策略） |
| 系统管理员 | 基础设施 | ✅ | ✅ | ✅ |

## 6. 认证与授权机制

- **服务间认证**：内部 API Key（`SM_INTERNAL_API_KEY`）经 ExternalSecret 从 Vault 注入，禁止硬编码。
- **用户认证**：对接统一 IAM（SM-IAM），OIDC/OAuth2 标准协议，JWT 令牌有效期 ≤ 2 小时。
- **授权模型**：RBAC + ABAC 混合策略，角色绑定资源范围，属性条件补充细粒度控制。
- **管理面认证**：管理接口仅在企业内网/VPN 后暴露，强制 MFA。

## 7. 审计日志要求

- 审计事件：登录、权限变更、数据导出、密钥操作、配置变更。
- 日志字段：时间戳、操作者、源 IP、操作类型、资源对象、结果、请求 ID。
- 留存周期：生产 ≥ 365 天，审计归档 ≥ 3 年。
- 完整性：日志写入后不可修改，异步转发至审计中心集中存储。
- 告警：连续失败认证、越权访问尝试、大批量数据导出触发实时告警。

## 8. 漏洞管理流程

1. **发现**：CI 流水线 Trivy 镜像扫描 + 依赖 SCA（Dependabot）+ 月度漏扫 + 外部情报订阅。
2. **定级**：CVSS v3.1 评分，Critical ≥ 9.0 / High 7.0-8.9 / Medium 4.0-6.9 / Low < 4.0。
3. **修复 SLA**：Critical 72 小时、High 7 天、Medium 30 天、Low 90 天。
4. **验证**：修复后回归扫描 + 渗透测试抽验。
5. **复盘**：Critical/High 漏洞须出具根因分析报告（RCA），更新安全基线。
